image: python:3.11-slim

stages:
  - format
  - lint
  - type_check
  - security
  - test
  - coverage
  - deploy

variables:
  PIP_CACHE_DIR: "$CI_PROJECT_DIR/.cache/pip"
  PYTHONPATH: "."
  PYTHONDONTWRITEBYTECODE: "1"
  PYTHONUNBUFFERED: "1"

cache:
  key: "$CI_COMMIT_REF_SLUG"
  paths:
    - .cache/pip
    - venv/

before_script:
  - python --version
  - pip install virtualenv
  - virtualenv venv
  - source venv/bin/activate
  - pip install --upgrade pip
  - pip install -r requirements.txt

# --- FORMAT STAGE ---
ruff-format-check:
  stage: format
  script:
    - ruff format --check .

pyupgrade-check:
  stage: format
  script:
    - pyupgrade --py311-plus **/*.py

# --- LINT STAGE ---
ruff-check:
  stage: lint
  script:
    - ruff check .

pylint-check:
  stage: lint
  script:
    - pylint **/*.py --fail-under=8.0

flake8-check:
  stage: lint
  script:
    - flake8 . --max-line-length=88 --extend-ignore=E203

vulture-check:
  stage: lint
  script:
    - vulture .

# --- TYPE CHECK STAGE ---
mypy-check:
  stage: type_check
  script:
    - mypy .

# --- SECURITY STAGE ---
security-bandit:
  stage: security
  script:
    - bandit -r . -ll --config pyproject.toml

security-semgrep:
  stage: security
  image: returntocorp/semgrep
  variables:
    SEMGREP_AGENT_TOKEN: $SEMGREP_AGENT_TOKEN
  before_script: []
  script:
    - semgrep ci --config auto

dependency-audit:
  stage: security
  script:
    - pip-audit

# --- TEST STAGE ---
unit-tests:
  stage: test
  script:
    - pytest

# --- COVERAGE STAGE ---
coverage-report:
  stage: coverage
  script:
    - pytest --cov=. --cov-report=xml --cov-report=term
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml

# --- DEPLOY STAGE ---
changelog:
  stage: deploy
  image:
    name: orhunp/git-cliff:latest
    entrypoint: [""]
  before_script:
    - apk add --no-cache git || true
  script:
    - git fetch --unshallow || true
    - git-cliff --output CHANGELOG.md
  artifacts:
    paths:
      - CHANGELOG.md
  only:
    - main
    - compliance-fix
